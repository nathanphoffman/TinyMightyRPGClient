services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: tmrpg
      POSTGRES_PASSWORD: tmrpg
      POSTGRES_DB: tmrpg
    ports:
      - "5432:5432"
    volumes:
      - tmrpg_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U tmrpg"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  tmrpg_postgres_data:
